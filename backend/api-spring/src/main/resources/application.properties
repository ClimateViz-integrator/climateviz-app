# Configuracion de la aplicacion
spring.application.name=API Clima

# Configuracion de la base de datos
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/climate_api
spring.datasource.username=root
spring.datasource.password=d123
# spring.datasource.url=${db_url}
# spring.datasource.username=${db_username}
# spring.datasource.password=${db_password}
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Configuracion de JWT (rutas a las llaves)
jwtKeys.privateKeyPath=jwtKeys/private.key.pem
jwtKeys.publicKeyPath=jwtKeys/public.key.pem

# Configuracion del servidor
server.port=9000